<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Book Collection</title>
  </head>
  <body>
    <main>
        <h1>Here are the results that matches your search:</h1>
        <ul>
          <% bananas.forEach(book => { %>
            <li><%= book.title %></li>
            <p>Author: <%= book.author %></p>
            <img src="<%= book.image %>" />
            <p>Description: <%= book.description %></p>
            <% }); %>
        </ul>
    </main>
  </body>
</html>